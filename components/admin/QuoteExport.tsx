"use client";
export default function QuoteExport({rows}:{rows:any[]}){function csv(){const h=["Quote No","Customer","Product","Currency","Unit Price","MOQ","Lead Time","Product Size","Package Size","Net Weight","Gross Weight","Qty/Carton","Carton Size","Carton N.W.","Carton G.W.","CBM","Notes"];const esc=(v:any)=>`"${String(v??"").replaceAll('"','""')}"`;const body=rows.map(r=>[r.quote_no,r.customer,r.product,r.currency,r.unit_price,r.moq,r.lead_time,r.packing_size?.product_size,r.packing_size?.package_size,r.packing_size?.net_weight,r.packing_size?.gross_weight,r.packing_size?.qty_carton,r.packing_size?.carton_size,r.packing_size?.carton_nw,r.packing_size?.carton_gw,r.packing_size?.cbm,r.notes].map(esc).join(","));const blob=new Blob(["\ufeff"+[h.map(esc).join(","),...body].join("\n")],{type:"text/csv;charset=utf-8"});const a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download=`xinshern-quotes-${new Date().toISOString().slice(0,10)}.csv`;a.click();URL.revokeObjectURL(a.href)}return <button className="btn-secondary" onClick={csv}>Export Complete CSV</button>}
