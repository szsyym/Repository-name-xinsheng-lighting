import type{SitePage}from"@/lib/types";
export default function PageForm({page,action}:{page:SitePage;action:(form:FormData)=>void}){return <form className="admin-form" action={action}><h2>{page.title}</h2><label>Admin Label / Page Title<input name="title" defaultValue={page.title}/></label><label>Eyebrow Text<input name="eyebrow" defaultValue={page.eyebrow}/></label><label>Main Heading<input name="heading" defaultValue={page.heading}/></label><label>Main Description<textarea name="body" defaultValue={page.body}/></label><label>External Video URL<input name="video_url" defaultValue={String(page.content?.video_url||"")} placeholder="YouTube or hosted video URL"/></label>{page.media?.length?<div className="admin-media-grid">{page.media.map(m=><div className="admin-media" key={m.url}>{m.type==="video"?<video src={m.url} controls/>:<img src={m.url} alt=""/>}</div>)}</div>:null}<label>Upload Page Images / Videos<input type="file" name="media" accept="image/*,video/*" multiple/></label><label className="check"><input type="checkbox" name="published" defaultChecked={page.published}/> Published</label><button className="btn-primary">Save {page.title}</button></form>}
