import type{GalleryItem}from"@/lib/types";
export default function GalleryCarousel({items,logos=false}:{items:GalleryItem[];logos?:boolean}){if(!items.length)return <div className="card" style={{padding:32,textAlign:"center",color:"var(--muted)"}}>Images can be added from the admin dashboard.</div>;return <div className="carousel-row">{items.map(i=><article className={`carousel-item ${logos?"logo-item":""}`} key={i.id}><div className="media">{i.media_type==="video"?<video src={i.media_url} controls/>:<img src={i.media_url} alt={i.title}/>}</div>{!logos&&<><h3>{i.title}</h3>{i.caption&&<p>{i.caption}</p>}</>}</article>)}</div>}
