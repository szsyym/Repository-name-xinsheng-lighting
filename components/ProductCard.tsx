import Link from "next/link";import type{Product}from"@/lib/types";export default function ProductCard({product}:{product:Product}){const cover=product.media?.[0],target=encodeURIComponent(product.slug||product.model||product.id);return <article className="card product-card"><Link href={`/products/${target}`}><div className="product-visual">{cover?.url?(cover.type==="video"?<video src={cover.url} muted/>:<img src={cover.url} alt={cover.alt||product.name}/>):<div className="mini-orb"/>}</div><div className="product-info">{product.model&&<small className="product-model">{product.model}</small>}<h3>{product.name}</h3><p>{product.short_description}</p><div className="tags">{product.features.slice(0,2).map(item=><span className="tag" key={item}>{item}</span>)}</div><div className="card-bottom"><strong>{product.moq?`MOQ: ${product.moq}`:"OEM / ODM"}</strong><span>View product →</span></div></div></Link></article>}
