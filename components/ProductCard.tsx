import Link from "next/link";import type{Product}from"@/lib/types";
export default function ProductCard({product}:{product:Product}){const cover=product.media?.[0];return <article className="card product-card"><Link href={`/products/${product.slug}`}><div className="product-visual">{cover?.url?(cover.type==="video"?<video src={cover.url} muted/>:<img src={cover.url} alt={cover.alt||product.name}/>):<div className="mini-orb"/>}</div><div className="product-info"><h3>{product.name}</h3><p>{product.short_description}</p><div className="tags">{product.features.slice(0,2).map(x=><span className="tag" key={x}>{x}</span>)}</div><div className="card-bottom"><strong>{product.moq?`MOQ: ${product.moq}`:"OEM / ODM"}</strong><span>View product →</span></div></div></Link></article>}
