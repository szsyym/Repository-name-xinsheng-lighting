"use client";
import { useState } from "react";
import type { MediaItem } from "@/lib/types";
export default function MediaGallery({items,name}:{items:MediaItem[];name:string}){const media=items.length?items.slice(0,8):[];const[active,setActive]=useState(0);if(!media.length)return <div className="main-media"><div className="light-orb" style={{margin:0}}/></div>;const current=media[Math.min(active,media.length-1)];return <div className="media-gallery"><div className="main-media">{current.type==="video"?<video src={current.url} controls playsInline/>:<img src={current.url} alt={current.alt||name}/>}</div><div className="thumbs">{media.map((item,index)=><button type="button" key={`${item.url}-${index}`} className={`thumb ${index===active?"active":""}`} onClick={()=>setActive(index)} aria-label={`Show ${name} image ${index+1}`}>{item.type==="video"?<video src={item.url} muted/>:<img src={item.url} alt={item.alt||`${name} ${index+1}`}/>}</button>)}</div></div>}
