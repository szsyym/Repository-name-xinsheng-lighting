"use client";
import{useState}from"react";
export default function ContactForm(){const[status,setStatus]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setStatus("Sending…");const form=new FormData(e.currentTarget);const r=await fetch("/api/inquiries",{method:"POST",body:form});const j=await r.json();setStatus(j.message);if(r.ok)e.currentTarget.reset()}return <form className="card form-card" onSubmit={submit}><div className="form-grid"><input className="field" name="full_name" placeholder="Full Name *" required/><input className="field" name="company_name" placeholder="Company Name *" required/><input className="field" name="email" type="email" placeholder="Email Address *" required/><input className="field" name="phone" placeholder="Phone / WhatsApp"/><select className="field full" name="inquiry_type" defaultValue=""><option value="" disabled>Inquiry Type</option><option>Product Purchase</option><option>OEM / ODM Request</option><option>Sample Request</option><option>Technical Support</option></select><select className="field full" name="annual_volume" defaultValue=""><option value="" disabled>Estimated Annual Volume</option><option>1,000–5,000 units</option><option>5,000–20,000 units</option><option>20,000–100,000 units</option><option>100,000+ units</option></select><textarea className="field full" name="message" placeholder="Tell us about your target market, specifications, requirements and timeline…" required/><button className="btn-primary full" type="submit">Send Inquiry — We Reply Within 24h</button><p className="form-status full">{status||"Your information is protected and used only to respond to your inquiry."}</p></div></form>}
