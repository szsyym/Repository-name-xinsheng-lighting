# XINSHERN 网站部署说明（Supabase + GitHub + Vercel）

## 已经部署网站：升级到 V2（本次更新）

如果旧版本已经在 Vercel 正常运行，只需完成下面 3 步：

1. 登录 Supabase → SQL Editor → New query。
2. 打开新版项目中的 `supabase/update-v2.sql`，复制全部内容并点击 Run。成功后会建立“产品分类”表并写入原有 8 个分类。
3. 用新版文件覆盖本地项目后，在 VS Code Terminal 运行：

```bash
git add .
git commit -m "Update scenes categories and product pages"
git push
```

GitHub 更新后，Vercel 会自动部署。部署成功后检查：

- `/scenes` 为独立场景页面。
- `/admin/categories` 可以新增产品分类。
- `/admin/customers` 可以发布客户合影和合作客户 Logo。
- 产品详情页仅在右上显示一次 Features，下面显示 Specifications、Packing Info、Package Included 和 Q&A。

本次更新不需要添加 Service Role Key，也不要删除 Vercel 中现有的 Supabase URL 和 Publishable/Anon Key。

本文档按新手操作顺序编写。先在本地确认网站，再连接 Supabase，然后上传 GitHub，最后部署 Vercel。

## 第一阶段：本地检查

1. 解压 ZIP。
2. 用 VS Code 打开解压后的 `xinsheng-lighting-cms` 文件夹。
3. VS Code 菜单点击 Terminal → New Terminal。
4. 输入：`npm install`
5. 输入：`cp .env.example .env.local`
6. 输入：`npm run dev`
7. 浏览器打开 `http://localhost:3000`。

此时没有连接 Supabase，前台会显示演示产品；后台登录暂时不能使用。

## 第二阶段：建立 Supabase 数据库

1. 登录 Supabase，创建一个新项目。
2. 左侧打开 SQL Editor，点击 New query。
3. 打开项目内 `supabase/schema.sql`，复制全部内容到 SQL Editor。
4. 点击 Run。数据库表、权限和 `media` 图片/视频空间会一次建立完成。
5. 左侧 Authentication → Users → Add user。
6. 建立你的后台管理员邮箱和密码，并勾选自动确认邮箱。
7. Supabase → Project Settings → API，复制：
   - Project URL
   - anon / public key
8. 打开本地 `.env.local`，填写：

```env
NEXT_PUBLIC_SUPABASE_URL=你的Project URL
NEXT_PUBLIC_SUPABASE_ANON_KEY=你的anon key
NEXT_PUBLIC_SITE_URL=http://localhost:3000
```

9. 关闭原 Terminal，再重新运行 `npm run dev`。
10. 打开 `http://localhost:3000/admin/login`，使用刚创建的管理员登录。

注意：不要把 Supabase 密码或 service_role key 发到聊天里，也不要提交到 GitHub。

## 第三阶段：上传 GitHub

1. GitHub 新建一个空 Repository，例如 `xinsheng-lighting-website`。
2. 不要勾选 README、.gitignore 或 License。
3. 在 VS Code Terminal 依次运行：

```bash
git init
git add .
git commit -m "Initial Xinshern website"
git branch -M main
git remote add origin 你的GitHub仓库地址
git push -u origin main
```

`.env.local` 已被 `.gitignore` 排除，不会上传。

## 第四阶段：部署 Vercel

1. 登录 Vercel，点击 Add New → Project。
2. 选择刚建立的 GitHub Repository，点击 Import。
3. Framework Preset 保持 Next.js。
4. 展开 Environment Variables，增加：
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - `NEXT_PUBLIC_SITE_URL`（先填写 Vercel 分配的网址，正式域名绑定后再更新）
5. 点击 Deploy。
6. 部署成功后打开网站，检查 Home、Products、Product Detail、Factory、FAQ、News、Contact 和 Admin。

## 第五阶段：后台发布内容

建议顺序：

1. Admin → Home / About / Factory：修改真实文字，上传页面图片和视频。
2. Admin → Scenes / Banners / Factory Media：上传 2–3 张首页 Banner、Lighting Scenes 和 Factory Gallery。
3. Admin → Product Categories：需要时增加第 9 个及更多产品分类。
4. Admin → Customer Photos & Logos：发布客户合影与合作客户 Logo；前台一次显示 3 张并可左右滚动。
5. Admin → Products：新增真实产品，填写产品图片/视频、Features、参数、装箱、清单和 FAQ。
6. Admin → News：发布新闻。
7. Admin → Inquiries：查看客户询盘。
8. Admin → Quotation Database：保存报价并导出完整 CSV。

## 后续绑定正式域名

网站内容确认后，在 Vercel 项目 → Settings → Domains 添加 `szxinshengtech.com` 和 `www.szxinshengtech.com`。正式切换前不要删除旧网站 DNS；先让我根据 Vercel 提示一步一步带你修改。
