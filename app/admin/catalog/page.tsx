import{createClient}from"@/lib/supabase/server";import{uploadCatalog}from"../actions";import type{GalleryItem}from"@/lib/types";
export default async function CatalogAdmin(){const s=createClient();const{data}=s?await s.from("site_media").select("*").eq("collection","catalog").order("created_at",{ascending:false}):{data:[]};const items=(data||[])as GalleryItem[];return <><div className="admin-actions"><div><h1>Catalog Management</h1><p>Upload a new PDF. The newest published catalog automatically powers the homepage download button.</p></div></div><form className="admin-form" action={uploadCatalog}><div className="admin-form-grid"><label>Catalog Name<input name="title" required placeholder="XINSHERN Lighting Catalog"/></label><label>Version / Language<input name="version" placeholder="2026 · English"/></label></div><label>Catalog PDF<input type="file" name="catalog" accept="application/pdf" required/></label><button className="btn-primary">Upload &amp; Publish Catalog</button></form><h2 style={{marginTop:38}}>Catalog History</h2>{items.length?<table className="admin-table"><thead><tr><th>Name</th><th>Version</th><th>Status</th><th>File</th></tr></thead><tbody>{items.map(i=><tr key={i.id}><td>{i.title}</td><td>{i.caption||"—"}</td><td>{i.published?"Current":"Archived"}</td><td><a className="accent" href={i.media_url} target="_blank">Open PDF ↗</a></td></tr>)}</tbody></table>:<div className="empty">No catalog uploaded yet.</div>}</>}
