import Link from"next/link";import{getNews}from"@/lib/queries";import{deleteNews}from"../actions";
export default async function AdminNews(){const posts=await getNews(undefined,true);return <><div className="admin-actions"><div><h1>News</h1><p>Publish company news, product updates and industry articles.</p></div><Link className="btn-primary" href="/admin/news/new">Add News</Link></div><table className="admin-table"><thead><tr><th>Title</th><th>Category</th><th>Date</th><th>Status</th><th>Actions</th></tr></thead><tbody>{posts.map(p=><tr key={p.id}><td>{p.title}</td><td>{p.category}</td><td>{p.published_at.slice(0,10)}</td><td>{p.status}</td><td><Link className="accent" href={`/admin/news/${p.id}`}>Edit</Link>{!p.id.startsWith("n")&&<form action={deleteNews.bind(null,p.id)} style={{display:"inline",marginLeft:15}}><button className="danger">Delete</button></form>}</td></tr>)}</tbody></table></>}
