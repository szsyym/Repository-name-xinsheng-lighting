import{createClient}from"@/lib/supabase/server";import{deleteQuote,saveQuote}from"../actions";import QuoteExport from"@/components/admin/QuoteExport";
const pack=["product_size","package_size","net_weight","gross_weight","qty_carton","carton_size","carton_nw","carton_gw","cbm"];
export default async function Quotes(){const s=createClient();const{data}=s?await s.from("quotes").select("*").order("created_at",{ascending:false}):{data:[]};const rows=data||[];return <><div className="admin-actions"><div><h1>Quotation Database</h1><p>Filter records and export an Excel-compatible quotation with formatting, formulas, merged title cells and all nine packing fields.</p></div></div><QuoteExport rows={rows}/><form id="new-quote" className="admin-form" action={saveQuote}><h2>+ Add Product / Quotation Record</h2><div className="admin-form-grid"><label>Quote No.<input name="quote_no" required/></label><label>Customer<input name="customer" required/></label><label>Category / Model<input name="product" required/></label><label>Currency<input name="currency" defaultValue="USD"/></label><label>Unit Price (USD)<input name="unit_price" type="number" step="0.0001"/></label><label>MOQ<input name="moq" type="number"/></label><label>Lead Time<input name="lead_time" placeholder="30–35 days"/></label>{pack.map(x=><label key={x}>{x.replaceAll("_"," ")}<input name={x}/></label>)}</div><label>Notes / Packing List<textarea name="notes"/></label><button className="btn-primary">Save Product to Quotation</button></form><h2 style={{marginTop:35}}>Saved Products</h2>{rows.length?<table className="admin-table"><thead><tr><th>No.</th><th>Customer</th><th>Category / Model</th><th>Price</th><th>MOQ</th><th>Packing</th><th>Action</th></tr></thead><tbody>{rows.map((r:any)=><tr key={r.id}><td>{r.quote_no}</td><td>{r.customer}</td><td>{r.product}</td><td>{r.currency} {r.unit_price}</td><td>{r.moq}</td><td>{r.packing_size?.package_size||"—"} / {r.packing_size?.qty_carton||"—"} pcs</td><td><form action={deleteQuote.bind(null,r.id)}><button className="danger">Delete</button></form></td></tr>)}</tbody></table>:<div className="empty">No quotation records yet.</div>}</>}
