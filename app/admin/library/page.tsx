import Link from"next/link";import{getContentEntries}from"@/lib/queries";import{deleteContentEntry}from"../actions";
export default async function Library(){const rows=await getContentEntries(undefined,true);return <><div className="admin-actions"><div><h1>Content &amp; Knowledge</h1><p>Solutions, technical articles, knowledge base, FAQ and customer cases.</p></div><Link className="btn-primary" href="/admin/library/new">Add Content</Link></div>{rows.length?<table className="admin-table"><thead><tr><th>Type</th><th>Title</th><th>Category</th><th>SEO / AI</th><th>Status</th><th>Actions</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.type}</td><td>{r.title}</td><td>{r.category||"—"}</td><td>{r.seo?.title?"SEO ✓":"SEO —"} · {r.ai_summary?"AI ✓":"AI —"}</td><td>{r.status}</td><td><Link className="accent" href={`/admin/library/${r.id}`}>Edit</Link><form action={deleteContentEntry.bind(null,r.id)} style={{display:"inline",marginLeft:15}}><button className="danger">Delete</button></form></td></tr>)}</tbody></table>:<div className="empty">No content yet.</div>}</>}
