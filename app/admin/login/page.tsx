"use client";
import{useState}from"react";import{useRouter}from"next/navigation";import{createClient}from"@/lib/supabase/client";
export default function Login(){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setError("");try{const{error}=await createClient().auth.signInWithPassword({email,password});if(error)throw error;router.push("/admin");router.refresh()}catch(e:any){setError(e.message||"Login failed")}}return <div className="admin-login"><form className="admin-form" onSubmit={submit}><div className="logo"><strong>XINSHERN</strong><small>ADMIN LOGIN</small></div><label>Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></label><label>Password<input value={password} onChange={e=>setPassword(e.target.value)} type="password" required/></label><button className="btn-primary">Sign In</button>{error&&<p className="danger">{error}</p>}<p className="hint">Create the first administrator in Supabase → Authentication → Users.</p></form></div>}
