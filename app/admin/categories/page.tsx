import{createCategory,deleteCategory}from"../actions";import{getCategories}from"@/lib/queries";
export default async function CategoriesAdmin(){const categories=await getCategories(true);return <><div className="admin-actions"><div><h1>Product Categories</h1><p>Add product categories beyond the original eight. Published categories appear in the product menu.</p></div></div><form className="admin-form" action={createCategory}><div className="admin-form-grid"><label>Category Name *<input name="name" required placeholder="e.g. Solar Lighting"/></label><label>URL Slug<input name="slug" placeholder="auto-created-from-name"/></label><label>Sort Order<input name="sort_order" type="number" defaultValue="0"/></label><label className="check"><input name="published" type="checkbox" defaultChecked/> Published</label></div><label>Description<textarea name="description" placeholder="Short category description for buyers and SEO"/></label><button className="btn-primary">Add Category</button></form><h2 style={{marginTop:38}}>Current Categories</h2><table className="admin-table"><thead><tr><th>Category</th><th>Slug</th><th>Order</th><th>Status</th><th>Action</th></tr></thead><tbody>{categories.map(c=><tr key={c.id}><td>{c.name}</td><td>{c.slug}</td><td>{c.sort_order}</td><td>{c.published?"Published":"Hidden"}</td><td>{!c.id.startsWith("default-")&&<form action={deleteCategory.bind(null,c.id)}><button className="danger">Delete</button></form>}</td></tr>)}</tbody></table></>}
