import {notFound} from "next/navigation";import RichContent from "@/components/RichContent";import {getNewsPost} from "@/lib/queries";export async function generateMetadata({params}:{params:{slug:string}}){const post=await getNewsPost(params.slug);return{title:post?.title||"News",description:post?.excerpt}}export default async function NewsPost({params}:{params:{slug:string}}){const post=await getNewsPost(params.slug);if(!post)notFound();return <article className="section"><div className="container content-prose"><div className="kicker">{post.category} · {new Date(post.published_at).toLocaleDateString()}</div><h1 className="display-title news-detail-title">{post.title}</h1><p className="lead news-detail-excerpt">{post.excerpt}</p>{post.cover_url&&<img className="article-cover news-article-cover" src={post.cover_url} alt={post.title}/>}<RichContent html={post.content}/></div></article>}
