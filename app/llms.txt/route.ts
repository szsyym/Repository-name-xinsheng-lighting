import{getContentEntries,getProducts}from"@/lib/queries";export async function GET(){const[products,entries]=await Promise.all([getProducts(),getContentEntries()]);const text=["# XINSHERN Lighting","Shenzhen Xinshern Technology Co., Ltd. is an OEM and ODM LED lighting manufacturer for global B2B buyers.","","## Product Categories",...[...new Set(products.map(p=>p.category))].map(x=>`- ${x}`),"","## Products",...products.map(p=>`- ${p.model?`${p.model} — `:""}${p.name}: ${p.ai_summary||p.short_description}`),"","## Knowledge and Solutions",...entries.map(x=>`- ${x.title}: ${x.ai_summary||x.excerpt}`),"","Contact: sales@szxinshengtech.com"].join("\n");return new Response(text,{headers:{"content-type":"text/plain; charset=utf-8"}})}
