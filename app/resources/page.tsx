import SectionHead from"@/components/SectionHead";import ContentGrid from"@/components/ContentGrid";import{getContentEntries}from"@/lib/queries";
export const metadata={title:"Lighting Resources",description:"Technical articles, application guides, FAQs and product knowledge from Xinshern Lighting."};export default async function Resources(){const all=await getContentEntries();const rows=all.filter(x=>["article","knowledge","case"].includes(x.type));return <section className="section"><div className="container"><SectionHead kicker="Knowledge Center" title="Technical" accent="Resources" description="Product guides, application knowledge, technical articles and customer cases."/>{rows.length?<ContentGrid items={rows} base="/resources"/>:<div className="empty-detail">Resources will appear here after they are published in the CMS.</div>}</div></section>}
