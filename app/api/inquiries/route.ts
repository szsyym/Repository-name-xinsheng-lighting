import{NextResponse}from"next/server";import{createClient}from"@/lib/supabase/server";
export async function POST(req:Request){try{const form=await req.formData();const payload={full_name:String(form.get("full_name")||""),company_name:String(form.get("company_name")||""),email:String(form.get("email")||""),phone:String(form.get("phone")||""),inquiry_type:String(form.get("inquiry_type")||""),annual_volume:String(form.get("annual_volume")||""),message:String(form.get("message")||""),status:"new"};if(!payload.full_name||!payload.email||!payload.message)return NextResponse.json({message:"Please complete the required fields."},{status:400});const supabase=createClient();if(!supabase)return NextResponse.json({message:"Demo mode: connect Supabase to save inquiries."});const{error}=await supabase.from("inquiries").insert(payload);if(error)throw error;return NextResponse.json({message:"Thank you. Your inquiry has been received."})}catch{return NextResponse.json({message:"Unable to send. Please email sales@szxinshengtech.com."},{status:500})}}
